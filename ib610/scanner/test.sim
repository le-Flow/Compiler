/*
ß
|
^
*/

public class Gaming {
    public static int x = 9 + 2 - 1;
    boolean print = false;
    String lit = "Hello &!# \n test";
    
    public static void method(int start, boolean noPrint) {
        // countdown §$ß
        for(int k = x; k >= 0) {
            if (print) {
                System.out.println(k - 1)
            }
        }
    }
}

// Tabs

/*
dfadfsafds
afsadf
dfadfsafds
*/

“” 
""
"&!#" 
"use \n to denote a newline character” 
"include a quote like this \" and a backslash like this \\” 
 
Beispiele für ungültige Stringliterale: 
 
 "unterminated 
"also unterminated \" 
"backslash followed by space: \ is not allowed" 
"bad escaped character: \a AND not terminated